        <div class="container">
            {{back-button}}
            <br/>
            <br/>
            <div class="row">
                <div class="col-md-4">
                    <img {{bind-attr src=image}} width="200" class="thumbs">
                </div>
                <div class="col-md-8">
                    <table class="table table-hover">
                        <tbody>
                            <tr>
                                <td>Name</td>
                                <td>{{name}}</td>
                            </tr>
                            <tr>
                                <td>Contains</td>
                                <td>{{description}}</td>
                            </tr>
                        </tbody>
                    </table>

                    <button type="button" {{action "addToCart"}} class="btn btn-primary">Add to Cart</button>
                </div>
            </div>
        </div>